# Telegram Guide

Telegram is handled only by the Python 3 Core Server. Planned commands are `status`, `low`, `out`, `report`, `summary`, and `help`. Inventory summaries include inline buttons for Excel and PDF generation.
